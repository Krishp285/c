#include<stdio.h>
#include<conio.h>
void main(){
	int a,i,b;
	clrscr();

	printf("\n the first number is ");
	scanf("%d",a);
		printf("\n the first number is ");
	scanf("%d",b);
	for(i=a;i<=b;i++){
		printf("\n the square of the number is %d",i*i);
			}
			getch();
			}