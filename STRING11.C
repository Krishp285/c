#include<stdio.h>
#include<conio.h>
#include<string.h>
void main(){
	char stmt[50];
	int i,flag=1,len,j;
	clrscr();
	printf("\nenter the string");
	gets(stmt);
	for(i=0;j=(len-1);i<len;i++;j--)
	{
	if(name[i]!=name[j])
	{
	flag=0;
	break;

	}
	}
	if(flag)
	{
	printf("\npallindrone");
	}
	else
	{
	printf("\n not pallindrone");
	}
	getch();
	}